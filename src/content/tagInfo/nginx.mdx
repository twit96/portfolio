---
slug: nginx
---
import Link from '@components/Link.astro';

<Link
  href="https://www.nginx.com/"
  isExternal={true}
>NGINX</Link>
is a web server (which is software used to deliver webpages from a host computer to a user's browser).
