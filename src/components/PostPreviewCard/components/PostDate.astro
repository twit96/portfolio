---
const { datePublished, dateUpdated } = Astro.props;
---

{
  (datePublished || dateUpdated) &&
  <em>
    {/* Prefer Updated Date, Fallback to Published Date */}
    { dateUpdated && <>Updated {dateUpdated}</> }
    { !dateUpdated && datePublished }
  </em>
}
